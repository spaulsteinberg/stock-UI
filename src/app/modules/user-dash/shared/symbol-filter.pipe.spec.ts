import { SymbolFilterPipe } from './symbol-filter.pipe';

describe('SymbolFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SymbolFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
